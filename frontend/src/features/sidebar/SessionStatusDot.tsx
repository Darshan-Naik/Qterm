/** Status pip on the session icon for needs-input and complete. */
export function SessionStatusDot() {
  return (
    <span
      className="pointer-events-none absolute -right-0.5 -top-0.5 size-1.5 rounded-full bg-emerald-400"
      aria-hidden
    />
  );
}
