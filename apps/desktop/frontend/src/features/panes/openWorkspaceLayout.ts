/** Sticky project headers inside the open-workspace scroll area. */
export const OPEN_PROJECT_STICKY_TOP = "top-0";

/** Covers sub-pixel gaps so scrolled rows don’t flash under sticky headers. */
export const OPEN_STICKY_SEAL =
  "shadow-[0_-1px_0_0_var(--background),0_1px_0_0_var(--background)]";
